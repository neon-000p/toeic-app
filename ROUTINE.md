# ルーティン設定

claude.ai のルーティン「時事英語TOEIC教材 to notion」の設定内容。

- スケジュール: `30 7,20 * * *`（UTC）＝ JST 5:30 と 16:30、1日2回
- リポジトリ: `neon-000p/toeic-app` を選択する（編集画面の「リポジトリを選択」）
- コネクター: Notion（当面は残す。後述の手順で外せる）

リポジトリを選択すると、実行時にこのリポジトリが作業ディレクトリとして使える。
教材の仕様は `skills/news-toeic-english.SKILL.md` にあり、**そちらが唯一の仕様書**。
ルーティンの指示文は「そのスキルを読んで従え」と書くだけに留める。こうしておけば、
教材の作り方を変えたいときはスキルを直して push すればよく、ルーティンの設定画面を
触る必要がない。

---

## 指示（この下をそのまま貼り付ける）

```
本日の時事英語TOEIC教材を作成する。

## 手順

1. リポジトリ内の `skills/news-toeic-english.SKILL.md` を読む。以降はその手順に
   厳密に従うこと。これが唯一の仕様書であり、この指示文とスキルが食い違った場合は
   スキルを優先する。

2. スキルのステップ0〜4に従い、7セクションの教材本文（Markdown）を作成する。
   ジャンルと文法トピックの重複判定には `data/news/history.json` を使う（Notion は見ない）。

3. スキルのステップ5に従って、リポジトリに書き出す。
   - `data/news/<id>.json` を新規作成する（`<id>` はステップ0で取得した JST の
     実時刻から `YYYY-MM-DD-HHMM` の形で作る）
   - `data/news/index.json` の `items` 先頭に1件追加し、
     `updatedAt` を JST の実時刻に更新する
   - `data/news/history.json` の `items` 先頭に、今回のジャンルと文法トピックを追記する
     （これを忘れると次回以降の重複判定が壊れる）
   - **`audio` は書かない。** 本文の読み上げ音声は GitHub Actions が別途作って足す
   - **`index.json` の件数は減らさない。** 古い教材の整理は `tools/prune.js` が毎朝行う
     （直近1年ぶんを残す）。ここで消すと、消えた教材の音声が片づけられずに残り続ける
   - **`main` ブランチに対して**作業し、`git add` → `git commit` → `git push origin main` まで行う。
     作業用ブランチを切ったりプルリクエストを作ったりしないこと（GitHub Pages は main だけを配信している）
   - コミットメッセージは `教材追加: <id> <ジャンル>` の形にする

4. `news-toeic-english-notion` スキルの手順に従い、2 で作った本文を Notion にも保存する。

## 注意

- スキルの中止条件に該当した場合は、教材を作らずに中止して報告する。
  その場合リポジトリにも Notion にも一切書き込まないこと。
- JSON が壊れているとアプリでその教材が開けなくなる。push の前に、スキル 5-6 の
  自己検証を必ず実施すること。
- 作業が終わったら、リポジトリに未コミットの変更が残っていないことを確認する。
```

---

## 動作確認の手順

設定を保存したら、ルーティン画面の再生ボタン（▷）で手動実行する。翌朝を待つ必要はない。

確認すること:

1. **`main` ブランチに** `data/news/<id>.json` が増えているか（作業ブランチに入っていないか）
2. `index.json` の先頭がその id になっているか
3. `history.json` の先頭に今回のジャンルと文法トピックが入っているか
4. https://neon-000p.github.io/toeic-app/ を開いて最新の教材が出るか
5. Notion 側もこれまでどおり保存されているか

うまくいかない場合の切り分け:

- **リポジトリに何も書かれない** → リポジトリが選択されていないか、クラウド実行環境から
  push できていない。実行履歴を開いて、どこで止まったかを読む
- **スキルを読めていない** → 指示文にスキル本文を直接貼り付ける方式に戻す
  （この方式にする前は、スキル全文を指示欄に貼っていた）
- **教材は出来たが JSON が壊れている** → スキルのステップ5-6 を強めるか、JSON だけ作り直す

---

## Notion への保存をやめるとき

上の手順が安定して動くようになったら、Notion を切れる。判定材料はすでに
`data/news/history.json` に移してあるので、**残っているのは保存先としての役割だけ**。

1. 指示文から **手順 4 を丸ごと削除**する
2. 編集画面の「コネクター」から **Notion を外す**
3. ルーティン名を「時事英語TOEIC教材」などに変える（任意）

Notion 側の既存ページは残しておいてよい。過去の教材アーカイブとして参照できる。

なお、**これでルーティンの本数は減らない**。もともと1本のルーティンが Notion と
GitHub の両方に書いていて、その片方をやめるだけだから。本数の枠を空けたい場合は、
別のルーティンを止める必要がある。

## Part 3・Part 4 の音声（GitHub Actions）

会話の音声は、あらかじめ作ってリポジトリに置く。再生のたびに作ると待たされるため。

**仕組み**

`data/part3/*.json` または `data/part4/*.json` が main に入ると `.github/workflows/part3-audio.yml` が動き、`tools/make-audio.js`（Part 3）と `tools/make-part4-audio.js`（Part 4）が Gemini TTS で音声を作る。**Part 5 は対象外**（端末の読み上げで鳴らすため）。できた MP3 は `data/part3/audio/<id>.mp3`・`data/part4/audio/<id>.mp3` に置かれ、セットの JSON に `audio` が足されて `[audio]` 付きのコミットで main に入る。ルーティン側は音声のことを何もしなくてよい。

**最初に1回だけ必要な準備**

1. GitHub の対象リポジトリで Settings → Secrets and variables → Actions → New repository secret
2. Name に `GEMINI_API_KEY`、Secret に Google AI Studio のキーを貼って保存

Secret は公開リポジトリでも中身は見えない。コードにも履歴にも残らない。

**手で動かす**

Actions タブ →「音声を作る」→ Run workflow。`force` を on にすると、すでに音声があるセットも作り直す。

**注意**

- 無料枠の TTS は分あたりの回数が小さい。`tools/make-audio.js` は呼び出しの間を 1.5 秒空け、429 なら API の指示秒数だけ待って最大3回やり直す。
- 1回の呼び出しで指定できる声は2人まで。3人の会話は「同時に出てくる話者が2人まで」のかたまりに切って作り、つなぐ。10行の3人会話で5回ほど。**Part 4 は話し手が1人**なので、トーク全体が1回で済む（＋前置き1回＋文ごとの本数）。
- アクセント（米・英・豪）は指定できない。`speakers[].accent` は端末読み上げに落ちたときだけ効く。
- **`Ms.` のような敬称の点は、音声を作る前に落としている**（`tools/tts-lib.js` の `speechText`）。
  点を残すと合成器がそこを文の終わりと見なし、「Hello, Ms.」で切って、本物の文末では切らずに
  続けてしまう。教材の JSON は直さない（画面には `Ms.` と出したい）ので、読み上げに送る文字列
  だけを直している。アプリ側の端末読み上げにも同じ処理がある（`core.js` の `TTS.forSpeech`）。
- 音声がまだ無いセットは端末の読み上げで再生される。学習は止まらない。
- runner に ffmpeg は入っていないので、ワークフローで apt から入れている。ffmpeg が無いと WAV のまま置かれ、1本2MB を超える。その状態でもう一度走らせれば、API を呼ばずに MP3 へ差し替える。

---

# Part 3 のルーティン（週1回・5セット）

時事英語とは**別のルーティン**として作る。片方が止まっても、もう片方に影響しないようにするため。

- 名前: `Part 3 会話問題を5セット`
- スケジュール: `0 20 * * 6`（UTC）＝ **JST 日曜 5:00、週1回**
- リポジトリ: `neon-000p/toeic-app` を選択する（編集画面の「リポジトリを選択」）
- コネクター: 不要（Web 検索も Notion も使わない）

時事ネタではないので毎日作る必要がない。週に1回5セット作り、1日1セットずつ解く想定。

教材の仕様は `skills/part3.SKILL.md` にあり、**そちらが唯一の仕様書**。ルーティンの指示文は「そのスキルを読んで従え」と書くだけに留める。作り方を変えたいときはスキルを直して push すればよく、ルーティンの設定画面を触らなくてよい。

## 指示（この下をそのまま貼り付ける）

```
TOEIC Part 3 の練習セットを5本作る。

## 手順

1. リポジトリ内の `skills/part3.SKILL.md` を読む。以降はその手順に厳密に従うこと。
   これが唯一の仕様書であり、この指示文とスキルが食い違った場合はスキルを優先する。

2. `data/part3/history.json` を読み、直近10件の場面を確認する。
   この回の5本は、直近10件と重ならず、かつ5本どうしも重ならない場面にする。

3. スキルのステップ0〜4に従い、5セットを作る。5本が似通わないよう、スキルの
   「この回の5本を散らす」の表（場面・話者・会話の型・設問の型・正解の位置）を必ず満たすこと。
   - 3人会話はちょうど1本
   - 会話の型は3種以上
   - 15問の正解が A・B・C・D それぞれ2問以上

4. スキルのステップ5に従って、リポジトリに書き出す。
   - `data/part3/<id>.json` を5本新規作成する（`<id>` は JST の実時刻から
     `YYYY-MM-DD-HHMM-N` の形。`N` は1〜5。連番を付けないと id がぶつかる）
   - `data/part3/index.json` の `items` 先頭に5件追加し、`no` は既存の最大値+1から順に振り、
     `updatedAt` を JST の実時刻に更新する。**古い項目は削除しない**
   - `data/part3/history.json` の `items` 先頭に5件追記する
     （これを忘れると次回以降の場面の重複判定が壊れる）
   - **`audio` は書かない。** 会話音声は GitHub Actions が別途作って足す
   - **`main` ブランチに対して**作業し、`git add` → `git commit` → `git push origin main` まで行う。
     作業用ブランチを切ったりプルリクエストを作ったりしないこと（GitHub Pages は main だけを配信している）
   - 5本を1つのコミットにまとめる。メッセージは `Part 3 追加: <日付> の5セット`

5. push の前に、必ず機械の確認を通す。落ちたら直してから push すること。

   node tools/check-part3.js --all

## 注意

- JSON が壊れているとアプリでそのセットが開けない。5本それぞれについて、スキル 5-5 の
  自己検証と上記の `check-part3.js` を必ず実施すること。
- 途中で作れなくなった場合は、**できた分だけを書き出して push し、何本作れたかを報告する**。
  5本そろわないことを理由に全部を捨てないこと。
- `data/news/` には一切触れないこと。時事英語は別のルーティンが管理している。
- 実在の企業名・製品名・人名は使わない。架空のものにする。
- 作業が終わったら、リポジトリに未コミットの変更が残っていないことを確認する。
```

## 音声について

ルーティンが5セットを push すると、GitHub Actions（`.github/workflows/part3-audio.yml`）が自動で音声を作る。**ルーティン側は音声のことを何もしない。**

5セットぶんで Gemini への呼び出しは **60〜70回**（会話のかたまり＋1行ずつ）。無料枠は分あたりの回数が小さいので、呼び出しの間隔を4秒空けている。**5〜8分ほど**かかる。

無料枠の上限に当たって途中で止まっても、**毎朝 5:00 のスケジュール実行が足りない分だけを拾う**ので、翌朝には揃う。音声がまだ無いセットは端末の読み上げで再生されるため、その間も学習は止まらない。

## 動作確認

1. ルーティンを保存したら、設定画面から手動で1回実行する
2. `data/part3/` に5本の JSON が増え、`index.json` が8件（既存3＋5）になっていることを確認
3. アプリの [実践] → [Part 3] を開き、`#4` 〜 `#8` が並ぶことを確認
4. 5〜10分後、`data/part3/audio/` に5本の MP3 と5つのフォルダが増えることを確認
5. どれか1セットを開き、会話が Gemini の声で流れ、対訳でも同じ声が鳴ることを確認

---

# Part 4 のルーティン（週1回・5セット）

Part 3 とも時事英語とも**別のルーティン**として作る。どれかが止まっても、他に影響しないようにするため。

- 名前: `Part 4 説明文問題を5セット`
- スケジュール: `0 21 * * 6`（UTC）＝ **JST 日曜 6:00、週1回**
- リポジトリ: `neon-000p/toeic-app` を選択する（編集画面の「リポジトリを選択」）
- コネクター: 不要（Web 検索も Notion も使わない）

Part 3 のルーティン（JST 日曜 5:00）と1時間ずらしてある。同時に走らせると、両方が
`main` に push しようとしてぶつかるため。

教材の仕様は `skills/part4.SKILL.md` にあり、**そちらが唯一の仕様書**。ルーティンの指示文は「そのスキルを読んで従え」と書くだけに留める。

## 指示（この下をそのまま貼り付ける）

```
TOEIC Part 4 の練習セットを5本作る。

## 手順

1. リポジトリ内の `skills/part4.SKILL.md` を読む。以降はその手順に厳密に従うこと。
   これが唯一の仕様書であり、この指示文とスキルが食い違った場合はスキルを優先する。

2. `data/part4/history.json` を読み、直近10件の場面とトークの種類を確認する。
   この回の5本は、直近10件と重ならず、かつ5本どうしも重ならない場面にする。

3. スキルのステップ0〜4に従い、5セットを作る。5本が似通わないよう、スキルの
   「この回の5本を散らす」の表（種類・場面・話し手・アクセント・設問の型・正解の位置）を必ず満たすこと。
   - talkType は5本とも別。優先枠3本・標準枠2本
   - 1問目は必ず gist か who
   - intent と infer を合わせて2本以上に入れる
   - 15問の正解が A・B・C・D それぞれ2問以上

4. スキルのステップ5に従って、リポジトリに書き出す。
   - `data/part4/<id>.json` を5本新規作成する（`<id>` は JST の実時刻から
     `YYYY-MM-DD-HHMM-N` の形。`N` は1〜5。連番を付けないと id がぶつかる）
   - `data/part4/index.json` の `items` 先頭に5件追加し、`no` は既存の最大値+1から順に振り、
     `kind`（種類の日本語）と `questions`（3）も入れ、`updatedAt` を JST の実時刻に更新する。
     **古い項目は削除しない**
   - `data/part4/history.json` の `items` 先頭に5件追記する
     （これを忘れると次回以降の場面の重複判定が壊れる）
   - **`audio` は書かない。** トーク音声は GitHub Actions が別途作って足す
   - **`main` ブランチに対して**作業し、`git add` → `git commit` → `git push origin main` まで行う。
     作業用ブランチを切ったりプルリクエストを作ったりしないこと（GitHub Pages は main だけを配信している）
   - 5本を1つのコミットにまとめる。メッセージは `Part 4 追加: <日付> の5セット`

5. push の前に、必ず機械の確認を通す。落ちたら直してから push すること。

   node tools/check-part4.js --all

## 注意

- JSON が壊れているとアプリでそのセットが開けない。5本それぞれについて、スキル 5-5 の
  自己検証と上記の `check-part4.js` を必ず実施すること。
- 途中で作れなくなった場合は、**できた分だけを書き出して push し、何本作れたかを報告する**。
  5本そろわないことを理由に全部を捨てないこと。
- `data/news/` と `data/part3/` には一切触れないこと。別のルーティンが管理している。
- 実在の企業名・製品名・人名は使わない。架空のものにする。
- 作業が終わったら、リポジトリに未コミットの変更が残っていないことを確認する。
```

## 音声について

ルーティンが5セットを push すると、GitHub Actions が自動で音声を作る。**ルーティン側は音声のことを何もしない。**

5セットぶんで Gemini への呼び出しは **55回ほど**（前置き5＋トーク5＋文ごと45前後）。Part 3 と同じ枠を使うので、両方が同じ日に走ると合計120回を超える。無料枠の上限に当たって途中で止まっても、**毎朝 5:00 のスケジュール実行が足りない分だけを拾う**ので、数日のうちには揃う。音声がまだ無いセットは端末の読み上げで再生されるため、その間も学習は止まらない。

## 動作確認

1. ルーティンを保存したら、設定画面から手動で1回実行する
2. `data/part4/` に5本の JSON が増え、`index.json` の件数が5件ぶん増えていることを確認
3. アプリの [実践] → [Part 4] を開き、新しい番号が並ぶことを確認
4. 5〜10分後、`data/part4/audio/` に5本の MP3 と5つのフォルダが増えることを確認
5. どれか1セットを開き、前置き → トークの順に Gemini の声で流れ、対訳でも同じ声が鳴ることを確認

---

# Part 5 のルーティン（週1回・5セット）

Part 3・Part 4 とも時事英語とも**別のルーティン**として作る。どれかが止まっても、他に影響しないようにするため。

- 名前: `Part 5 短文穴埋めを5セット`
- スケジュール: `0 22 * * 6`（UTC）＝ **JST 日曜 7:00、週1回**
- リポジトリ: `neon-000p/toeic-app` を選択する（編集画面の「リポジトリを選択」）
- コネクター: 不要（Web 検索も Notion も使わない）

Part 3（JST 日曜 5:00）・Part 4（6:00）と1時間ずつずらしてある。同時に走らせると、複数が
`main` に push しようとしてぶつかるため。

教材の仕様は `skills/part5.SKILL.md` にあり、**そちらが唯一の仕様書**。

## 指示（この下をそのまま貼り付ける）

```
TOEIC Part 5 の練習セットを5本作る。1セットは10問。

## 手順

1. リポジトリ内の `skills/part5.SKILL.md` を読む。以降はその手順に厳密に従うこと。
   これが唯一の仕様書であり、この指示文とスキルが食い違った場合はスキルを優先する。

2. `data/part5/history.json` を読み、直近10セットの文法トピック（topics）を確認する。
   この回の5セットは、直近10セットと同じトピックを3つ以上重ねない。

3. スキルのステップ1〜3に従い、5セット（合計50問）を作る。各セットで必ず満たすこと。
   - 文法6〜7問・語彙3〜4問。form（品詞）は2問、同じ型は最大3問
   - 文法の型は3種以上
   - 10問の正解が A・B・C・D それぞれ2問以上
   - 1文は12〜28語、空所（-------）は1文に1か所

4. スキルのステップ4に従って、リポジトリに書き出す。
   - `data/part5/<id>.json` を5本新規作成する（`<id>` は JST の実時刻から
     `YYYY-MM-DD-HHMM-N` の形。`N` は1〜5）
   - `data/part5/index.json` の `items` 先頭に5件追加し、`no` は既存の最大値+1から順に振り、
     `kind`（focus と同じ文字列）と `questions`（10）も入れ、`updatedAt` を更新する。
     **古い項目は削除しない**
   - `data/part5/history.json` の `items` 先頭に5件追記する（topics を忘れない）
   - **`audio` は書かない。** Part 5 に音声は無い
   - **`main` ブランチに対して**作業し、`git add` → `git commit` → `git push origin main` まで行う。
     作業用ブランチを切ったりプルリクエストを作ったりしないこと
   - 5本を1つのコミットにまとめる。メッセージは `Part 5 追加: <日付> の5セット`

5. push の前に、必ず機械の確認を通す。落ちたら直してから push すること。

   node tools/check-part5.js --all

## 注意

- JSON が壊れているとアプリでそのセットが開けない。スキル 4-5 の自己検証と
  上記の `check-part5.js` を必ず実施すること。
- 途中で作れなくなった場合は、**できた分だけを書き出して push し、何本作れたかを報告する**。
- `data/news/`・`data/part3/`・`data/part4/` には一切触れないこと。別のルーティンが管理している。
- 実在の企業名・製品名・人名は使わない。架空のものにする。
- 作業が終わったら、リポジトリに未コミットの変更が残っていないことを確認する。
```

## 音声について

**Part 5 は音声ファイルを作らない。** Actions も動かない（`data/part5/` はワークフローの対象に入れていない）。
解説ステップの ▶ は端末（Chrome / Edge）の読み上げで鳴らす。声と速さはアプリの ⚙ で選べる。

## 動作確認

1. ルーティンを保存したら、設定画面から手動で1回実行する
2. `data/part5/` に5本の JSON が増え、`index.json` の件数が5件ぶん増えていることを確認
3. アプリの [実践] → [Part 5] を開き、新しい番号が並ぶことを確認
4. どれか1セットを開き、10問を解いて採点まで進めることを確認
5. 解説ステップの ▶ で読み上げが鳴ることを確認

---

# Part 6 のルーティン（週1回・5セット）

他のパートとも時事英語とも**別のルーティン**として作る。

- 名前: `Part 6 長文穴埋めを5セット`
- スケジュール: `0 23 * * 6`（UTC）＝ **JST 日曜 8:00、週1回**
- リポジトリ: `neon-000p/toeic-app` を選択する
- コネクター: 不要

Part 3（5:00）・Part 4（6:00）・Part 5（7:00）と1時間ずつずらしてある。同時に走らせると、
複数が `main` に push しようとしてぶつかるため。

教材の仕様は `skills/part6.SKILL.md` にあり、**そちらが唯一の仕様書**。

## 指示（この下をそのまま貼り付ける）

```
TOEIC Part 6 の練習セットを5本作る。1セットは文書1本＋空所4つ。

## 手順

1. リポジトリ内の `skills/part6.SKILL.md` を読む。以降はその手順に厳密に従うこと。
   これが唯一の仕様書であり、この指示文とスキルが食い違った場合はスキルを優先する。

2. `data/part6/history.json` を読み、直近10件の docType と scene を確認する。
   この回の5本は、直近10件と重ならず、かつ5本どうしも重ならない場面にする。

3. スキルのステップ1〜4に従い、5セットを作る。各セットで必ず満たすこと。
   - docType は5本とも別。email と notice を1本ずつ入れる
   - 空所は本文に4か所。1段落に4つ固めない
   - 一文挿入（sentence）がちょうど1問。選択肢は4つとも完全な文（8〜18語）
   - 4問のうち2問以上は、空所のある文だけでは決まらない問題にする
   - 残り3問は文法1〜2問・語彙1〜2問。同じ型を2問入れない
   - 4問の正解に同じ文字が3問以上入らないようにする

4. スキルのステップ5に従って、リポジトリに書き出す。
   - `data/part6/<id>.json` を5本新規作成する（`<id>` は JST の実時刻から
     `YYYY-MM-DD-HHMM-N` の形。`N` は1〜5）
   - `data/part6/index.json` の `items` 先頭に5件追加し、`no` は既存の最大値+1から順に振り、
     `kind`（種類の日本語）と `questions`（4）も入れ、`updatedAt` を更新する。
     **古い項目は削除しない**
   - `data/part6/history.json` の `items` 先頭に5件追記する
   - **`audio` は書かない。** Part 6 に音声ファイルは持たない
   - **`main` ブランチに対して**作業し、`git add` → `git commit` → `git push origin main` まで行う。
     作業用ブランチを切ったりプルリクエストを作ったりしないこと
   - 5本を1つのコミットにまとめる。メッセージは `Part 6 追加: <日付> の5セット`

5. push の前に、必ず機械の確認を通す。落ちたら直してから push すること。

   node tools/check-part6.js --all

## 注意

- JSON が壊れているとアプリでそのセットが開けない。スキル 5-5 の自己検証と
  上記の `check-part6.js` を必ず実施すること。
- 途中で作れなくなった場合は、**できた分だけを書き出して push し、何本作れたかを報告する**。
- `data/news/`・`data/part3/`・`data/part4/`・`data/part5/` には一切触れないこと。
- 実在の企業名・製品名・人名は使わない。架空のものにする。
- 作業が終わったら、リポジトリに未コミットの変更が残っていないことを確認する。
```

## 音声について

**Part 6 は音声ファイルを作らない。** Actions も動かない（`data/part6/` はワークフローの対象外）。
対訳ステップの ▶ は端末（Chrome / Edge）の読み上げで鳴らす。読むのは空所を正解で埋めた文。

## 動作確認

1. ルーティンを保存したら、設定画面から手動で1回実行する
2. `data/part6/` に5本の JSON が増え、`index.json` の件数が5件ぶん増えていることを確認
3. アプリの [実践] → [Part 6] を開き、新しい番号が並ぶことを確認
4. どれか1セットを開き、空所に番号が振られていること、一文挿入の空所が幅いっぱいの
   下線になっていること、4問を解いて採点まで進めることを確認

---

# Part 7 のルーティン（週1回・5セット）

他のパートとも時事英語とも**別のルーティン**として作る。

- 名前: `Part 7 読解を5セット`
- スケジュール: `0 0 * * 0`（UTC）＝ **JST 日曜 9:00、週1回**
- リポジトリ: `neon-000p/toeic-app` を選択する
- コネクター: 不要

Part 3（5:00）・Part 4（6:00）・Part 5（7:00）・Part 6（8:00）と1時間ずつずらしてある。

教材の仕様は `skills/part7.SKILL.md` にあり、**そちらが唯一の仕様書**。

## 指示（この下をそのまま貼り付ける）

```
TOEIC Part 7 の練習セットを5本作る。

## 手順

1. リポジトリ内の `skills/part7.SKILL.md` を読む。以降はその手順に厳密に従うこと。
   これが唯一の仕様書であり、この指示文とスキルが食い違った場合はスキルを優先する。

2. `data/part7/history.json` を読み、直近10件の scene と docTypes を確認する。
   この回の5本は、直近10件と重ならず、かつ5本どうしも重ならない場面にする。

3. スキルのステップ1〜4に従い、5セットを作る。必ず満たすこと。
   - single（文書1本＋2〜4問）を3本、double（文書2本＋5問）を2本
   - double には、両方の文書を見ないと解けない設問（cross）を1問以上入れる
   - 設問の順番を本文の流れに合わせる（gist と notTrue は例外）
   - 同じ型は最大2問、gist は0〜1問
   - insert（文の位置）は single のセットにだけ最大1問。入れるなら本文に [1]〜[4] を置く
   - intent（意図）は chat の文書があるときだけ
   - 正解に同じ文字が3問以上入らないようにする

4. スキルのステップ5に従って、リポジトリに書き出す。
   - `data/part7/<id>.json` を5本新規作成する（`<id>` は JST の実時刻から
     `YYYY-MM-DD-HHMM-N` の形。`N` は1〜5）
   - `data/part7/index.json` の `items` 先頭に5件追加し、`no` は既存の最大値+1から順に振り、
     `format`・`kind`（文書の種類を ＋ でつないだ日本語）・`questions` も入れ、
     `updatedAt` を更新する。**古い項目は削除しない**
   - `data/part7/history.json` の `items` 先頭に5件追記する
   - **`audio` は書かない。** Part 7 に音声ファイルは持たない
   - **`main` ブランチに対して**作業し、`git add` → `git commit` → `git push origin main` まで行う。
     作業用ブランチを切ったりプルリクエストを作ったりしないこと
   - 5本を1つのコミットにまとめる。メッセージは `Part 7 追加: <日付> の5セット`

5. push の前に、必ず機械の確認を通す。落ちたら直してから push すること。

   node tools/check-part7.js --all

## 注意

- JSON が壊れているとアプリでそのセットが開けない。スキル 5-5 の自己検証と
  上記の `check-part7.js` を必ず実施すること。
- 途中で作れなくなった場合は、**できた分だけを書き出して push し、何本作れたかを報告する**。
- `data/news/` と `data/part3/`〜`data/part6/` には一切触れないこと。
- 実在の企業名・製品名・人名は使わない。架空のものにする。
- 作業が終わったら、リポジトリに未コミットの変更が残っていないことを確認する。
```

## 音声について

**Part 7 は音声ファイルを作らない。** Actions も動かない（`data/part7/` はワークフローの対象外）。
対訳ステップの ▶ は端末（Chrome / Edge）の読み上げで鳴らす。

## 動作確認

1. ルーティンを保存したら、設定画面から手動で1回実行する
2. `data/part7/` に5本の JSON が増え、`index.json` の件数が5件ぶん増えていることを確認
3. アプリの [実践] → [Part 7] を開き、新しい番号が並ぶことを確認
4. 複数文書のセットを開き、文書が2枚に分かれて見出しが付くこと、解説で「2つの文書」の印が
   付いた設問があること、根拠の引用に文書名が添えられることを確認

---

# まとめて作るルーティン（週2回・1回7セット）

Part 3〜7 を**1本のルーティンでまとめて作る**。パートごとに5本ずつ作る前節までのルーティン
（5本）は、これに置き換えられる。**この節のルーティンを使うなら、前節までの5本は止めてよい。**

- 名前: `TOEIC 実践対策 まとめて作成`
- スケジュール: `0 20 * * 2,6`（UTC）＝ **JST 水曜 5:00 と 日曜 5:00、週2回**
- リポジトリ: `neon-000p/toeic-app` を選択する
- コネクター: 不要

## 1回に作る量

| パート | 1回 | 週（2回） |
|---|---|---|
| Part 3 会話問題 | 2セット | 4セット |
| Part 4 説明文 | 2セット | 4セット |
| Part 5 短文穴埋め | 1セット（10問） | 2セット |
| Part 6 長文穴埋め | 1セット（文書1本＋4問） | 2セット |
| Part 7 読解 | 1セット | 2セット |

1回で7セット。1日1セット解く想定なら、週14セットで十分に足りる。

**本数についてはこの指示文が各スキルより優先する。** スキルに書かれている「1回5セット」は
読み替えること。教材の中身の作り方（場面の選び方・設問の型・語注の基準など）はスキルに従う。

**Gemini の呼び出しは1回あたり50回ほど**（Part 3 と Part 4 の音声だけ）。パートごとに5本ずつ
作っていたときより軽いので、無料枠の上限に当たりにくい。

## 指示（この下をそのまま貼り付ける）

```
TOEIC の実践対策を、Part 3 から Part 7 までまとめて作る。

## この回に作る本数（各スキルの「1回5セット」より、こちらを優先する）

- Part 3（会話問題）: 2セット
- Part 4（説明文）: 2セット
- Part 5（短文穴埋め）: 1セット（10問）
- Part 6（長文穴埋め）: 1セット（文書1本＋空所4つ）
- Part 7（読解）: 1セット

本数以外（場面の選び方・設問の型・選択肢・語注・Point の書き方）は、各パートのスキルに
厳密に従うこと。スキルと本指示文が食い違うのは本数と、下の「散らし方」だけである。

## 手順

0. JST の実時刻を取得する。`TZ=Asia/Tokyo date` を単独で信用しないこと。

   node -e "console.log(new Intl.DateTimeFormat('sv-SE',{timeZone:'Asia/Tokyo',dateStyle:'short',timeStyle:'short'}).format(new Date()))"

   この時刻から `<id>` を作る。2本作るパートは `YYYY-MM-DD-HHMM-1` と `-2`、
   1本だけのパートは連番を付けず `YYYY-MM-DD-HHMM` とする。

1. Part 3 を2セット作る。`skills/part3.SKILL.md` に従う。
   - `data/part3/history.json` の直近10件と重ならない場面を2つ選ぶ。2本も別の場面にする
   - **話者**: `history.json` の直近6件を見て、`speakers` が3のセットが1本も無ければ、
     この回の1本を3人会話にする。1本でもあれば2本とも2人会話にする
   - 会話の型（問題と解決／比較と決定／依頼と調整／報告と指示）は2本で別にする
   - 2本×3問＝6問の正解が、同じ文字3問以上にならないようにする

2. Part 4 を2セット作る。`skills/part4.SKILL.md` に従う。
   - `talkType` は2本とも別にし、少なくとも1本は優先枠（voicemail / announcement / meeting）から選ぶ
   - 各セットの1問目は必ず gist か who
   - `intent` と `infer` を、2本のうち1本以上に入れる
   - 話し手は2本で M と W に分け、accent も別にする
   - 6問の正解が、同じ文字3問以上にならないようにする

3. Part 5 を1セット作る。`skills/part5.SKILL.md` に従う。
   - 10問。文法6〜7問・語彙3〜4問、form（品詞）は2問、同じ型は最大3問
   - 10問の正解が A・B・C・D それぞれ2問以上
   - `data/part5/history.json` の直近10件と同じ文法トピックを3つ以上重ねない

4. Part 6 を1セット作る。`skills/part6.SKILL.md` に従う。
   - 直近10件と違う `docType` と場面にする
   - 空所4つ、うち一文挿入がちょうど1問、空所の文だけでは決まらない問題が2問以上
   - 4問の正解が同じ文字3問以上にならないようにする

5. Part 7 を1セット作る。`skills/part7.SKILL.md` に従う。
   - `data/part7/history.json` の直近3件に `double` が無ければ `double`（文書2本＋5問）、
     あれば `single`（文書1本＋2〜4問）にする
   - `double` なら、両方の文書を見ないと解けない設問（cross）を1問以上入れる
   - `single` で `insert`（文の位置）を入れるなら、本文に [1]〜[4] を置く

6. 書き出しは**パートごとに**行い、そのつど確認して push する。
   まとめて最後に push すると、途中で止まったときに何も残らない。

   各パートで、次の順に行うこと。

   a. `data/partN/<id>.json` を作る
   b. `data/partN/index.json` の `items` 先頭に追加する。`no` は既存の最大値+1から順に振り、
      そのパートの決まりに従って `speakers` / `kind` / `talkType` / `format` / `questions` を入れ、
      `updatedAt` を JST の実時刻に更新する。**古い項目は削除しない**
   c. `data/partN/history.json` の `items` 先頭に追記する（重複判定に使うので省略しない）
   d. 機械の確認を通す。落ちたら直す

      node tools/check-partN.js --all

   e. `git pull --rebase origin main` を実行してから、`git add data/partN` →
      `git commit -m "Part N 追加: <日付> の<本数>セット"` → `git push origin main`

   **`git pull --rebase` を忘れないこと。** Part 3 と Part 4 を push すると音声を作る
   GitHub Actions が動き、main に別のコミットを足す。そのままでは次の push が弾かれる。

7. すべて終わったら、作ったセットの一覧（パートと id）を報告する。

## 注意

- **`audio` はどのパートでも書かない。** Part 3 と Part 4 の音声は Actions が作る。
  Part 5・6・7 に音声ファイルは無い（端末の読み上げで鳴らす）。
- **`main` ブランチに対して作業する。** 作業用ブランチを切ったりプルリクエストを作ったりしない。
- 途中で作れなくなった場合は、**そこまでに push できた分を残し、どのパートを何本作れたかを報告する**。
  残りを無理に詰め込まない。
- `data/news/` には一切触れないこと。時事英語は別のルーティンが管理している。
- 実在の企業名・製品名・人名は使わない。架空のものにする。
- 作業が終わったら、リポジトリに未コミットの変更が残っていないことを確認する。
```

## なぜパートごとに push するか

7セットを1回で作ると実行が長くなり、途中で止まることがある。まとめて最後に push する形だと、
そこで作ったものがすべて消える。パートごとに push しておけば、Part 5 で止まっても Part 3 と
Part 4 は残る。

その代わり、**push のたびに `git pull --rebase origin main` が要る**。Part 3 と Part 4 の push で
音声の Actions が動き、`読み上げ音声を追加 [audio]` というコミットを main に足すためである。

## 動作確認

1. ルーティンを保存したら、設定画面から手動で1回実行する
2. `data/part3/` に2本、`data/part4/` に2本、`data/part5/` `data/part6/` `data/part7/` に
   1本ずつ増えていることを確認
3. アプリの [実践] を開き、各パートの番号が増えていることを確認
4. 5〜10分後、`data/part3/audio/` と `data/part4/audio/` に音声が増えることを確認
5. Part 3 の1本が3人会話になっているか（直近6件に3人会話が無かった場合）を確認
